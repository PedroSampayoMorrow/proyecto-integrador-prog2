<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The UdesSA Network</title>
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css" 
        crossorigin="anonymous"
        referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/stylesheets/index.css">
</head>

<body>
    <%- include('./partials/header') %> 
    <main>
        <section class="center main-content">
            <% for( let i = 0; i < datos.length; i++ ) { %>
                <article class="post">
                    <div class="info-usuario ">
                        <img class = "imgComentario"src=<%= datos[i].imagenUrlAutor %> alt="">
                        <h4>
                            <a href="/usuario/detalle/id/<%= datos[i].idAutor %> "> <%= datos[i].usuarioAutor %> </a>
                        </h4>
                    </div>
                    <div class="post-body">
                        <a href="/posteo/detalle/id/<%= datos[i].id%>">
                            <img src="<%= datos[i].imagenUrl %> " alt="">
                        </a>
                    </div>
                    <ul class="post-reacciones">
                        <li>
                            <i class="fa-regular fa-heart"></i>
                        </li>
                        <li>
                            <i class="fa-regular fa-comment"></i>
                        </li>
                    </ul>
                    <div class="post-comentarios">
                        <h4>Comentarios</h4>
                        <ul>
                            <% for( let i2 = 0; i2 < datos[i].comentarios.length; i2++ ) { %>
                                <li>
                                    <a href="/usuario/detalle/id/<%= datos[i].comentarios[i2].idUsuario %>"><%= datos[i].comentarios[i2].usuarioAutor %></a>
                                    <span><%= datos[i].comentarios[i2].texto %></span>
                                </li>
                            <% } %>
                            <a href="/posteo/detalle/id/<%= datos[i].id%>">Ver más...</a>
                        </ul>
                    </div>
                </article>
            <% } %>
            
        </section>    
    </main>

    <%- include('./partials/footer') %> 



</body>

</html>